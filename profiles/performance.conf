# Performance: balanced + performance power profile and memory tuning checks.
PROFILE_DESCRIPTION=Balanced + performance power profile, zram, storage maintenance
MODULES=base,packages,dotfiles,terminal,shell,optimize
PKG_GROUPS=essentials,power
OPT_PROFILE=performance
