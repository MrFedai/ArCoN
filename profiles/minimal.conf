# Minimal: update the system, install the v2.5 essentials, safe cleanup only.
PROFILE_DESCRIPTION=Base update + essential packages, no desktop/security changes
MODULES=base,packages,optimize
PKG_GROUPS=essentials
PKG_USE_CUSTOM=no
OPT_PROFILE=minimal
